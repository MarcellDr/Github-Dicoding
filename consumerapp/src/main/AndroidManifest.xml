<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.marcelldr.consumerapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="com.marcelldr.githubdicoding.READ_DATABASE"/>
    <uses-permission
        android:name="com.marcelldr.githubdicoding.WRITE_DATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubDicoding">
        <activity android:name=".activity.DetailActivity" />
        <activity android:name=".activity.FavoriteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

    <queries>
        <package android:name="com.marcelldr.githubdicoding" />
    </queries>
</manifest>